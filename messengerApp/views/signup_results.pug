doctype html
html 
    head 
        title Signup Results 
    body    
        if(type == "error")
            h3(style = "color:red") #{message}
        else 
            h3 New User, #{user.username} has been created!